public class Man2 {
    /*private String previousLastName;
    boolean isRetired = true;
    private String partner;
    private boolean toPreviousLastName;

    public Man2(String firstName, String lastName, String partner, int age, boolean isMarried) {
        super(firstName, lastName, partner, age, isMarried);
    }

    // Variant 2
    public boolean isRetiredVar2() {
        return getAge() >= 65;
    } // does not do anything, if I print

    public void registerPartnership(Man2 man2) {
        this.partner = getPartner();
        this.setLastName(man2.getLastName());
        this.isMarried = true;
        System.out.println("Partner is: " + this.partner + ", Last name: " + this.lastName + ", Is married: " + this.isMarried);
    }

    public void deregisterPartnership(boolean returnToPreviousLastName, Man2 man2) {
        if (returnToPreviousLastName) {
            this.lastName = getLastName();
            //this.setLastName(previousLastName);
        }
        this.partner = null;
        System.out.println("After divorce: " + this.previousLastName + ", Lastname: " + this.lastName + ", To previous last name - " + returnToPreviousLastName);
    }*/

}
